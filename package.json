{
  "name": "giai-tri-music-player",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-scripts": "5.0.0"
  },
  "scripts": {
    "start": "react-scripts start",  // Start the development server
    "build": "react-scripts build",  // Build the app for production
    "test": "react-scripts test",    // Run tests
    "eject": "react-scripts eject",  // Eject configuration
    "lint": "eslint . --fix"        // Lint the code with auto-fix option
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not op_mini all"
  ]
}